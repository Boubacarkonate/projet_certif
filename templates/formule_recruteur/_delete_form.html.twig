<form method="post" action="{{ path('app_formule_recruteur_delete', {'id': formule_recruteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formule_recruteur.id) }}">
    <button class="btn">Delete</button>
</form>
